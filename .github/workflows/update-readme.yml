name: Update README with Repos
on:
  schedule:
    - cron: '0 8 * * 1' # Executa toda segunda-feira às 08:00 UTC
  workflow_dispatch: # Permite execução manual

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout código
        uses: actions/checkout@v3

      - name: Gerar lista de repositórios e atualizar README
        uses: athul/waka-readme@master
        with:
          SHOW_PROJECTS: true
          SHOW_TITLE: false
          PROJECTS_SORT_BY: "stars"
          MAX_PROJECTS: 6
          COMMIT_MESSAGE: "📦 Atualização automática: projetos em destaque"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
